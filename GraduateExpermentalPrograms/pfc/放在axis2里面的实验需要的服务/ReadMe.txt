Excel文件夹为pfc服务决策表，实验时需要导入

PFCService/PFCService2为实验服务，不同版本内置不同约束违反错误；每个服务里面有如下文档：
lib中存放数据库连接包，很重要，别删除
META-INF中存放的是WSDL和service.xml 超级超级重要！！！！ 其他地方放的和它一样的名字的不要管，以这个和tomcat中的为准 我已保证这两个是完全一致
ws里面存放的是 服务可执行class文档 如果服务后续需要进行更新，请更新它，更新完毕后，更新WSDL与service.xml！！

这个文件夹外面的其他文件，是使用AXIS2插件进行服务开发时生成的，没用没用不用管,东西都是原始的，早就不知道改过几个版本了，用了一定崩溃！